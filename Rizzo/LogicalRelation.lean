import Rizzo.LogicalRelation.Basic
import Rizzo.LogicalRelation.Core
import Rizzo.LogicalRelation.Properties
import Rizzo.LogicalRelation.Fundamental
import Rizzo.LogicalRelation.Functorial

/-!
The development of the logical relation is split across these modules:
`Basic` defines `lfp` and its reasoning principles, `Core` defines the
logical relation(s), `Properties` proves basic properties of the
logical relation(s), `Fundamental` proves lemmas for the proof of the
fundamental property, `Functorial` proves semantic functoriality of
open types.
-/
